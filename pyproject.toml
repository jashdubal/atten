[project]
name = "atten-tts"
version = "0.2.0"
description = "Atten's backward-compatible offline Kokoro speech backend"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "kokoro>=0.7.16",
    "soundfile>=0.13.1",
]
